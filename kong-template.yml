_format_version: "3.0"
_transform: true

services:
  - name: organizations-service
    url: "${ORGANIZATIONS_URL}"
    routes:
      # CRUD
      - name: get-organizations-route
        paths:
          - "/api/v1/organizations"
        strip_path: false

      - name: get-organization-by-id-route
        paths:
          - "~/api/v1/organizations/\\d+"
        strip_path: false

      - name: create-organization-route
        paths:
          - "/api/v1/organizations"
        strip_path: false

      - name: update-organization-route
        paths:
          - "~/api/v1/organizations/\\d+"
        strip_path: false

      - name: delete-organization-route
        paths:
          - "~/api/v1/organizations/\\d+"
        strip_path: false

      # Usuarios de una organización
      - name: get-users-by-organization-route
        paths:
          - "~/api/v1/organizations/\\d+/users"
        strip_path: false


  - name: drim-users-service
    url: "${DRIM_USERS_URL}"
    routes:
      # Auth
      - name: login-route
        paths:
          - "/api/v1/auth/login"
        strip_path: false

      - name: drim-logout-route
        paths:
          - "/api/v1/auth/logout"
        strip_path: false

      # CRUD
      - name: get-users-route
        paths:
          - "/api/v1/users"
        strip_path: false

      - name: get-user-by-id-route
        paths:
          - "~/api/v1/users/\\d+"
        strip_path: false

      - name: create-user-route
        paths:
          - "/api/v1/users"
        strip_path: false

      - name: update-user-route
        paths:
          - "~/api/v1/users/\\d+"
        strip_path: false

      - name: delete-user-route
        paths:
          - "~/api/v1/users/\\d+"
        strip_path: false

      # Role
      - name: get-roles-route
        paths:
          - "/api/v1/roles"
        strip_path: false

      - name: get-role-by-id-route
        paths:
          - "~/api/v1/roles/\\d+"
        strip_path: false

      - name: patch-user-role-route
        paths:
          - "~/api/v1/users/\\d+/roles/\\d+"
        strip_path: false

      # UserStatus
      - name: update-user-status-route
        paths:
          - "~/api/v1/users/\\d+/status/\\d+"
        strip_path: false
